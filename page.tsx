import {redirect} from "next/navigation";import {isAdmin} from "@/lib/auth";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){if(await isAdmin())redirect("/admin");const {error}=await searchParams;return <main className="denied"><div className="login-card"><h1>Log Masuk Admin</h1><p>Gunakan akaun pentadbir website sekolah.</p>{error&&<p className="login-error">E-mel atau kata laluan tidak betul.</p>}<form action="/api/admin/login" method="post"><label>E-mel<input type="email" name="email" required/></label><label>Kata laluan<input type="password" name="password" required/></label><button className="save-button" type="submit">Log masuk</button></form><a href="/">Kembali ke website</a></div></main>}
